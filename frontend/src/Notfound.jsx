//renders when page isn't found or if there's a connection error

export default function NotFound(){
  return (
    <div>
      <h1>404 - Page Not Found</h1>
      <p>The page you are looking for does not exist or can't be opened.</p>
    </div>
  );
};